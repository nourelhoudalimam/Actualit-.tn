<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/nav.ejs') %>
   
    <div class="text-center center-div" id="login">
      <div class="container w-25 border py-5">
          <div class="title pb-5">
              <h2 class="font-weight-bold"> Systéme de connexion</h2>
              <h6>Connecter en tant que administrateur</h6>
              <% if(locals.logout){ %>
                  <div class="alter alert-success text-center">
                      <%= logout %>
                  </div>
              <% } %>
          </div>
          <form action="/router/Admin-login" method="POST" class="pt-3">
              <div class="form-group">
                <small class="form-text text-muted text-left">entrer l'email de l'administrateur</small>  
                <input type="email" class="form-control" placeholder="email" name="email">
                 
              </div>
              <div class="form-group">
                <small class="form-text text-muted text-left">entrer le mot de passe</small> 
                <input type="password" class="form-control" name="password" placeholder="password">
                 
                 </div>
              <button type="submit" class="btn btn-success rounded-pill">Connecter</button>
              <% if(locals.error){ %>
                <div class="alert text-center">
                    <%= error %>
                </div>
            <% } %>
        </form>
      </div>
  </div>
  
      <%- include('./partials/footer.ejs') %>
      <style>
        @import url('https://fonts.googleapis.com/css2?family=Open+Sans&display=swap');

     .alter{font-size: 12px;
        font-weight: 600;color:green;
     }
     .alert{font-size: 12px;
        font-weight: 900;color:rgb(150, 5, 5);
     }
     .font-weight-bold{
        transform: translateY(-90%);
     }
   
body{
    font-family: 'Open Sans', sans-serif;
   
   }
.text-center{transform: translateY(40%);
    background-image: url('/public/back.jpg');
    background-size: cover;
    background-repeat: no-repeat;
}
.center-div{
    padding-top: 10%;
}

.btn.btn-success{
    padding: .4em 2em;
    background-color: red;
    color:white;cursor: pointer;
    margin-top: 20px;
    border-color: red;
}
.btn.btn-success:hover{
    
    background-color: white;
    color:red;border-color: red;
}
form{
    padding: 0 3em;
}
.form-group{
    margin-top: 20px;
}
.form-group input{
   
        outline: none;
        display: flex;
       
        width: 90%;
        padding: 10px 20px;
        font-weight: 400;
        border: 1px solid #607d8b;
        letter-spacing: 1px;
        background: white;
        color:#777;
        border-radius: 30px;
        font-size: 16px;
        border-bottom-width: 2px;
        transition: all 0.3s ease;
    } 
    .form-group input:focus{
        border-color:white; 
    }
     
.form-group input:hover{
        border-color:red; 
        background-color: white;
        color:#777
    } 
#login .title > span{
    font-size: .8em;
}
      </style>
  </body>
  </html >

